# `ex-paninij-maven-setup`

Compile the project using `mvn compile`, and run the executable with
`mvn exec:java`.

Notice that the sources generated by the annotation processor are placed in
`target/generated-source/java`.
